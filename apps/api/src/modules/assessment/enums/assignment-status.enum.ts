/** ExamAssignment state machine statuses. */
export enum AssignmentStatus {
  Draft = 'draft',
  Scheduled = 'scheduled',
  Active = 'active',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
