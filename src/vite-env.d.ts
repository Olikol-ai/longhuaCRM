/// <reference types="vite/client" />

/**
 * Подключает типы Vite для `import.meta.env`.
 *
 * Без этого TypeScript не знает о переменных окружения,
 * которые Vite отдаёт frontend-коду через `import.meta.env`.
 */
