import StatCard from '@/components/dashboard/StatCard';

/**
 * Analytics KPI card — wraps existing StatCard (SSOT for dashboard metrics).
 */
export function AnalyticsCard(props) {
  return <StatCard {...props} />;
}
